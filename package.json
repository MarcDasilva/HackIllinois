{
  "name": "lava-entropy-oracle",
  "version": "1.0.0",
  "description": "On-chain entropy seed oracle: snapshots Solana token accounts, computes SHA3-256 entropy, commits via Memo program.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node --project tsconfig.json src/index.ts",
    "dev:server": "ts-node --project tsconfig.json src/server.ts",
    "dev:client": "npm run dev --prefix frontend",
    "dev:all": "concurrently --names server,ui --prefix-colors yellow,magenta \"npm run dev:server\" \"npm run dev:client\"",
    "bootstrap:tokens": "ts-node scripts/bootstrap.ts",
    "start": "node dist/index.js",
    "start:server": "node dist/server.js",
    "verify": "ts-node --project tsconfig.json src/verifyMemo.ts"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@solana/spl-token": "^0.4.14",
    "@solana/web3.js": "^1.95.3",
    "@supabase/supabase-js": "^2.98.0",
    "@types/cors": "^2.8.19",
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "googleapis": "^171.4.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^20.14.0",
    "concurrently": "^9.2.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "keywords": [
    "solana",
    "entropy",
    "oracle",
    "devnet"
  ],
  "license": "MIT"
}
