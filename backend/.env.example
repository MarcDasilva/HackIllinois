# Copy from project root .env or set these for the backend server.
# Required for /transfer and /harden/*/by-id (Supabase Storage). Optional for /health.

SERVER_PORT=3001
SUPABASE_URL=https://your-project.supabase.co
# IMPORTANT: Use the SERVICE ROLE key (secret), NOT the anon/public key.
# Supabase Dashboard → Project Settings → API → service_role (reveal and copy).
# Paste the full JWT with no quotes; it is long.
SUPABASE_SERVICE_KEY=your-service-role-key
SUPABASE_TABLE=documents
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3001/oauth/callback

# Free mint: backend pays Solana tx fee. Required for POST /mint/commit.
# Path to a keypair JSON file (array of 64 numbers). Create with: solana keypair new
# Then fund on devnet: solana airdrop 1 --url devnet
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_KEYPAIR_PATH=~/.config/solana/id.json
