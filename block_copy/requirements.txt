# VeilDoc Dependencies

# Minimum Python version: 3.7

# Core dependencies (all from standard library for DOCX support):
# - argparse (CLI parsing)
# - hashlib (deterministic hashing)
# - json (sidecar file format)
# - os, pathlib (file operations)
# - random (seeded randomization)
# - re (pattern matching)
# - shutil (file operations)
# - sys (CLI interface)
# - tempfile (temporary directories)
# - xml.etree.ElementTree (XML parsing)
# - zipfile (DOCX archive handling)
# - datetime (timestamps)

# Optional: For PDF support
PyMuPDF>=1.23.0  # Also known as 'fitz' - PDF manipulation library

# Optional: For development and testing
# pytest>=7.0.0  # Uncomment if you want to add unit tests
# black>=22.0.0  # Uncomment for code formatting

